{
  "name": "telegram-vk-max-crossposting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js start",
    "start:test": "node index.js start --env .env.test",
    "check": "node index.js check",
    "check:test": "node index.js check --env .env.test",
    "max:chats": "node index.js max:chats",
    "max:chats:test": "node index.js max:chats --env .env.test",
    "test": "node --test"
  },
  "dependencies": {
    "undici": "^6.25.0"
  }
}
